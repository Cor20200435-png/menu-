```html
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>La Casa Dulce - Menú Interactivo</title>
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Google Fonts: Playfair Display (Serif Elegante) & Quicksand -->
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,500;0,600;0,700;0,800;1,500;1,600&family=Quicksand:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        dulceCream: '#FAF6F0',
                        dulceBrownDark: '#4A3525',
                        dulceBrownLight: '#5C4033',
                        dulceGold: '#C89D7C',
                        dulceGoldHover: '#B58966',
                        dulceGreen: '#3E5634',
                        dulceGreenHover: '#2E4325',
                        dulceAccent: '#B2533E',
                        dulceBox: '#EFEAE2',
                    },
                    fontFamily: {
                        serif: ['Playfair Display', 'serif'],
                        sans: ['Quicksand', 'sans-serif'],
                    }
                }
            }
        }
    </script>
    <style>
        body {
            background-color: #FAF6F0;
            color: #4A3525;
            font-family: 'Quicksand', sans-serif;
            -webkit-tap-highlight-color: transparent;
        }

        /* Animaciones del simulador */
        @keyframes dropTopping {
            0% {
                transform: translateY(-150px) scale(0.3) rotate(0deg);
                opacity: 0;
            }
            60% {
                transform: translateY(15px) scale(1.1) rotate(15deg);
                opacity: 1;
            }
            85% {
                transform: translateY(-8px) scale(0.95) rotate(-10deg);
            }
            100% {
                transform: translateY(0) scale(1) rotate(0deg);
                opacity: 1;
            }
        }

        @keyframes dripDown {
            0% { transform: scaleY(0); transform-origin: top; }
            100% { transform: scaleY(1); transform-origin: top; }
        }

        @keyframes pulseSoft {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.03); }
        }

        .topping-drop {
            animation: dropTopping 0.7s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
        }

        .sauce-drip {
            animation: dripDown 0.9s cubic-bezier(0.1, 0.8, 0.3, 1) forwards;
        }

        .pulse-premium {
            animation: pulseSoft 2s infinite ease-in-out;
        }

        /* Estilos de scroll personalizado */
        ::-webkit-scrollbar {
            width: 6px;
        }
        ::-webkit-scrollbar-track {
            background: #FAF6F0;
        }
        ::-webkit-scrollbar-thumb {
            background: #C89D7C;
            border-radius: 10px;
        }
    </style>
</head>
<body class="min-h-screen pb-16">

    <!-- PORTADA / HERO BANNER (Basado en la foto 2 de alta elegancia) -->
    <div class="relative max-w-4xl mx-auto mt-0 md:mt-4 overflow-hidden rounded-b-[40px] md:rounded-[40px] bg-dulceBrownDark shadow-2xl text-white text-center px-6 py-12 md:py-16">
        <!-- Overlay sutil con patrón de fondo -->
        <div class="absolute inset-0 opacity-5 bg-[radial-gradient(#C89D7C_1px,transparent_1px)] [background-size:16px_16px]"></div>
        
        <!-- Contenido principal -->
        <div class="relative z-10 flex flex-col items-center">
            
            <!-- Logo minimalista de La Casa Dulce -->
            <div class="w-20 h-20 bg-white rounded-full border border-dulceGold p-1.5 shadow-md flex items-center justify-center mb-4 transition-transform hover:rotate-6">
                <!-- Mini versión elegante de tu logo original -->
                <svg viewBox="0 0 200 200" class="w-full h-full text-dulceGreen">
                    <circle cx="100" cy="100" r="95" fill="none" stroke="currentColor" stroke-width="2" />
                    <circle cx="100" cy="100" r="90" fill="none" stroke="currentColor" stroke-width="1" stroke-dasharray="3 3" />
                    <g transform="translate(112, 45) scale(0.55)">
                        <path d="M 12,0 C 25,0 28,15 20,28 C 15,36 9,38 0,38 C -9,38 -15,36 -20,28 C -28,15 -25,0 -12,0 Z" fill="#B2533E"/>
                        <circle cx="0" cy="12" r="1" fill="#FAF6F0"/>
                        <path d="M -15,3 C -8,5 -3,-5 0,2 C 3,-5 8,5 15,3 C 10,8 6,8 0,5 C -6,8 -10,8 -15,3 Z" fill="currentColor"/>
                    </g>
                    <!-- Casita -->
                    <path d="M 45,100 Q 100,45 155,100 Q 140,100 135,95 Q 100,60 65,95 Q 60,100 45,100 Z" fill="currentColor" />
                    <rect x="118" y="70" width="10" height="22" fill="currentColor"/>
                    <path d="M 90,120 A 10,10 0 0 1 110,120 L 110,135 L 90,135 Z" fill="currentColor" />
                    <path d="M 50,140 Q 100,128 150,140" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"/>
                </svg>
            </div>

            <span class="text-xs font-bold text-dulceGold uppercase tracking-[0.3em] mb-1">Postres Artesanales</span>
            <h1 class="font-serif text-4xl md:text-5xl font-extrabold tracking-wider leading-tight">La Casa Dulce</h1>
            
            <p class="text-dulceGold/90 font-serif italic text-sm max-w-md mx-auto mt-3 font-medium leading-relaxed">
                "Cremosidad perfecta en cada capa, elaborados a mano con puro amor."
            </p>

            <span class="inline-flex items-center gap-1.5 mt-5 px-4 py-1.5 rounded-full border border-dulceGold/30 bg-white/5 text-xs font-bold text-dulceGold tracking-wider uppercase backdrop-blur-sm">
                ✨ Presentación Única de 8 Onzas
            </span>
        </div>
    </div>

    <!-- MENÚ DE NAVEGACIÓN (TABS ELEGANTES - Basados en la foto 2) -->
    <div class="max-w-md mx-auto mt-6 px-4">
        <div class="flex justify-between items-center bg-[#EFEAE2] p-1.5 rounded-2xl shadow-inner gap-1">
            
            <button id="tabButton_carta" onclick="switchTab('carta')" class="flex-1 py-3 px-4 rounded-xl text-xs font-bold uppercase tracking-wider transition-all flex items-center justify-center gap-2 bg-dulceBrownDark text-white shadow-md">
                📖 Carta
            </button>
            
            <button id="tabButton_armar" onclick="switchTab('armar')" class="flex-1 py-3 px-4 rounded-xl text-xs font-bold uppercase tracking-wider transition-all flex items-center justify-center gap-2 text-dulceBrownDark/70 hover:bg-white/40">
                🧁 Armar
            </button>
            
            <button id="tabButton_carrito" onclick="switchTab('carrito')" class="flex-1 py-3 px-4 rounded-xl text-xs font-bold uppercase tracking-wider transition-all flex items-center justify-center gap-2 text-dulceBrownDark/70 hover:bg-white/40 relative">
                🛒 Carrito
                <span id="badgeCarritoCount" class="absolute -top-1 -right-1 bg-dulceAccent text-white text-[10px] w-5 h-5 rounded-full flex items-center justify-center font-bold hidden">0</span>
            </button>
            
        </div>
    </div>

    <!-- CONTENIDO DE LAS PESTAÑAS -->
    <main class="max-w-4xl mx-auto px-4 mt-6">

        <!-- ==================== PESTAÑA 1: LA CARTA ==================== -->
        <section id="tabContent_carta" class="space-y-6">
            
            <!-- EXPLICACIÓN: ¿Qué es nuestro Pavé? (Basado exactamente en la segunda foto) -->
            <div class="bg-white rounded-3xl p-6 shadow-sm border border-dulceBox text-center max-w-2xl mx-auto">
                <span class="text-[10px] font-bold text-dulceGold uppercase tracking-widest block mb-1">Especialidad de la casa</span>
                <h3 class="font-serif text-xl font-bold text-dulceBrownDark mb-3">¿Qué es nuestro Pavé?</h3>
                <p class="text-xs md:text-sm text-dulceBrownLight leading-relaxed font-medium">
                    Es un postre frío tradicional, armado delicadamente capa por capa con una textura ultra cremosa que se deshace en tu boca, contrastando con toques crujientes y salsas artesanales únicas.
                </p>
            </div>

            <!-- TÍTULO DE SECCIÓN -->
            <div class="flex items-center justify-between border-b border-dulceBox pb-3 mt-8">
                <h3 class="font-serif text-2xl font-extrabold text-dulceBrownDark flex items-center gap-2">
                    🌟 Pavés Premium <span class="text-xs bg-dulceGold/20 text-dulceBrownDark px-2.5 py-1 rounded-full font-sans font-bold">$14.000</span>
                </h3>
            </div>

            <!-- GRILLA DE PAVÉS PREMIUM -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                <!-- Premium 1: Fresas -->
                <div class="bg-white rounded-3xl p-5 shadow-sm border border-dulceBox flex flex-col justify-between hover:shadow-md transition-all">
                    <div>
                        <div class="flex justify-between items-start mb-2">
                            <span class="text-3xl">🍓</span>
                            <span class="text-[9px] bg-dulceAccent/10 text-dulceAccent font-extrabold px-2 py-0.5 rounded-full uppercase">Fresa Especial</span>
                        </div>
                        <h4 class="font-serif text-lg font-bold text-dulceBrownDark mb-1">Pavé de Fresas con Crema</h4>
                        <p class="text-xs text-dulceBrownLight/80 font-medium mb-4 leading-relaxed">
                            Fresas frescas seleccionadas, crema especial de la casa, queso rallado prémium y lluvia de lecherita. (Incluye 2 salsas de tu elección).
                        </p>
                    </div>
                    <div class="space-y-2 mt-2">
                        <button onclick="comprarPostrePredefinido('Pavé de Fresas con Crema', 14000, 'Fresas 🍓', 'Queso 🧀')" class="w-full bg-dulceBrownDark hover:bg-dulceBrownLight text-white py-2.5 rounded-xl text-xs font-bold transition-all flex items-center justify-center gap-1.5">
                            ➕ Añadir al Carrito
                        </button>
                        <button onclick="personalizarPostreEnSimulador('premium', 'fresas')" class="w-full border border-dulceGold text-dulceGold hover:bg-dulceCream py-2.5 rounded-xl text-xs font-bold transition-all">
                            🎨 Personalizar en vivo
                        </button>
                    </div>
                </div>

                <!-- Premium 2: Oblea -->
                <div class="bg-white rounded-3xl p-5 shadow-sm border border-dulceBox flex flex-col justify-between hover:shadow-md transition-all">
                    <div>
                        <div class="flex justify-between items-start mb-2">
                            <span class="text-3xl">🥞</span>
                            <span class="text-[9px] bg-dulceAccent/10 text-dulceAccent font-extrabold px-2 py-0.5 rounded-full uppercase">Crocante Star</span>
                        </div>
                        <h4 class="font-serif text-lg font-bold text-dulceBrownDark mb-1">Pavé de Oblea</h4>
                        <p class="text-xs text-dulceBrownLight/80 font-medium mb-4 leading-relaxed">
                            Trocitos de oblea crujiente, queso rallado de alta calidad, arequipe cremoso y nuestra famosa salsa de mora artesanal. (Incluye 2 salsas).
                        </p>
                    </div>
                    <div class="space-y-2 mt-2">
                        <button onclick="comprarPostrePredefinido('Pavé de Oblea Especial', 14000, 'Oblea 🥞', 'Queso 🧀')" class="w-full bg-dulceBrownDark hover:bg-dulceBrownLight text-white py-2.5 rounded-xl text-xs font-bold transition-all flex items-center justify-center gap-1.5">
                            ➕ Añadir al Carrito
                        </button>
                        <button onclick="personalizarPostreEnSimulador('premium', 'oblea')" class="w-full border border-dulceGold text-dulceGold hover:bg-dulceCream py-2.5 rounded-xl text-xs font-bold transition-all">
                            🎨 Personalizar en vivo
                        </button>
                    </div>
                </div>

                <!-- Premium 3: Maracuyá -->
                <div class="bg-white rounded-3xl p-5 shadow-sm border border-dulceBox flex flex-col justify-between hover:shadow-md transition-all">
                    <div>
                        <div class="flex justify-between items-start mb-2">
                            <span class="text-3xl">💛</span>
                            <span class="text-[9px] bg-dulceAccent/10 text-dulceAccent font-extrabold px-2 py-0.5 rounded-full uppercase">Frutal Ácido</span>
                        </div>
                        <h4 class="font-serif text-lg font-bold text-dulceBrownDark mb-1">Pavé de Maracuyá</h4>
                        <p class="text-xs text-dulceBrownLight/80 font-medium mb-4 leading-relaxed">
                            Delicada y sedosa crema artesanal de maracuyá hecha en casa, coronada con un baño de dulce lecherita de alta calidad. (Incluye 2 salsas).
                        </p>
                    </div>
                    <div class="space-y-2 mt-2">
                        <button onclick="comprarPostrePredefinido('Pavé de Crema de Maracuyá', 14000, 'Crema de Maracuyá 💛')" class="w-full bg-dulceBrownDark hover:bg-dulceBrownLight text-white py-2.5 rounded-xl text-xs font-bold transition-all flex items-center justify-center gap-1.5">
                            ➕ Añadir al Carrito
                        </button>
                        <button onclick="personalizarPostreEnSimulador('premium', 'maracuya')" class="w-full border border-dulceGold text-dulceGold hover:bg-dulceCream py-2.5 rounded-xl text-xs font-bold transition-all">
                            🎨 Personalizar en vivo
                        </button>
                    </div>
                </div>
            </div>

            <!-- TÍTULO DE SECCIÓN CLÁSICOS -->
            <div class="flex items-center justify-between border-b border-dulceBox pb-3 mt-10">
                <h3 class="font-serif text-2xl font-extrabold text-dulceBrownDark flex items-center gap-2">
                    ✨ Pavés Clásicos <span class="text-xs bg-dulceGreen/10 text-dulceGreen px-2.5 py-1 rounded-full font-sans font-bold">$13.000</span>
                </h3>
            </div>

            <!-- TARJETA PAVÉ CLÁSICO -->
            <div class="bg-white rounded-3xl p-6 shadow-sm border border-dulceBox max-w-2xl mx-auto flex flex-col md:flex-row justify-between items-center gap-6">
                <div class="space-y-2 text-center md:text-left flex-1">
                    <span class="text-3xl">🥣</span>
                    <h4 class="font-serif text-xl font-bold text-dulceBrownDark">Pavé Base Tradicional</h4>
                    <p class="text-xs text-dulceBrownLight/80 leading-relaxed font-medium">
                        Disfruta de la crema de pavé clásica más suave. Esta opción te otorga derecho de cortesía para seleccionar **1 salsa** y **1 topping clásico** completamente incluidos en el precio base.
                    </p>
                </div>
                <div class="flex flex-col gap-2 w-full md:w-auto min-w-[200px]">
                    <button onclick="switchTab('armar'); seleccionarBaseWizard('clasico')" class="w-full bg-dulceGreen hover:bg-dulceGreenHover text-white py-3 rounded-xl text-xs font-bold transition-all flex items-center justify-center gap-1">
                        🧁 Diseñar e Incluir Sabores
                    </button>
                </div>
            </div>

            <!-- ADICIONES Y TABLA DE PRECIOS -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mt-10">
                <!-- Salsas -->
                <div class="bg-[#FAF6F0] border-2 border-dashed border-dulceGold/30 p-5 rounded-2xl">
                    <h4 class="font-serif text-md font-bold text-dulceBrownDark mb-3 text-center">🍯 Salsas que puedes incluir</h4>
                    <ul class="text-xs text-dulceBrownLight font-bold space-y-2 text-center">
                        <li>🍮 Arequipe Cream</li>
                        <li>🍫 Chocolate Artesanal</li>
                        <li>🥛 Dulce Lecherita</li>
                        <li>🍇 Salsa de Mora Natural de la Casa</li>
                    </ul>
                </div>
                <!-- Toppings -->
                <div class="bg-[#FAF6F0] border-2 border-dashed border-dulceGold/30 p-5 rounded-2xl">
                    <h4 class="font-serif text-md font-bold text-dulceBrownDark mb-3 text-center">🍫 Toppings Extras Disponibles</h4>
                    <div class="grid grid-cols-2 gap-3 text-[11px] text-dulceBrownLight font-bold text-center">
                        <div>
                            <span class="text-dulceAccent font-extrabold uppercase text-[9px] tracking-wider block mb-1">Clásicos (+ $2.000)</span>
                            Milo 🤎<br>Leche Klim 🥛<br>Brownie 🧁<br>Ducales 🍪<br>Quipito 🎉
                        </div>
                        <div>
                            <span class="text-dulceAccent font-extrabold uppercase text-[9px] tracking-wider block mb-1">Premium (+ $3.000)</span>
                            Oblea 🥞<br>Queso 🧀<br>Fresas 🍓<br>Crema de Maracuyá 💛
                        </div>
                    </div>
                </div>
            </div>

            <!-- ==================== SECCIÓN DE INVITACIÓN A COMPARTIR EN REDES ==================== -->
            <div class="bg-gradient-to-br from-dulceBrownDark to-[#35251a] text-white rounded-3xl p-6 md:p-8 shadow-xl border border-dulceGold/30 max-w-2xl mx-auto text-center space-y-4 mt-12 relative overflow-hidden">
                <div class="absolute -right-12 -bottom-12 opacity-10 text-9xl pointer-events-none">📸</div>
                <div class="absolute -left-12 -top-12 opacity-10 text-9xl pointer-events-none">💬</div>
                
                <span class="text-xs font-bold text-dulceGold uppercase tracking-[0.2em] block">¡Tu opinión nos llena el corazón!</span>
                <h3 class="font-serif text-xl md:text-2xl font-bold">✨ Comparte tu Experiencia Dulce ✨</h3>
                <p class="text-xs md:text-sm text-dulceCream/90 leading-relaxed font-medium max-w-lg mx-auto">
                    ¿Qué te pareció la cremosidad de tu Pavé? ¡Queremos saberlo! Graba un video divertido o tómale una foto espectacular a tu postre, compártelo en tus redes etiquetándonos y ayúdanos a endulzar el día de más personas.
                </p>
                <div class="pt-2 flex justify-center gap-4 flex-wrap">
                    <a href="https://instagram.com/La_casadulce1" target="_blank" class="px-5 py-2 bg-gradient-to-r from-purple-500 via-pink-500 to-yellow-500 rounded-full text-xs font-bold transition-all hover:scale-105 inline-flex items-center gap-1.5">
                        📸 Instagram: @La_casadulce1
                    </a>
                    <a href="https://tiktok.com/@la.casa.dulce62" target="_blank" class="px-5 py-2 bg-white text-black rounded-full text-xs font-bold transition-all hover:scale-105 inline-flex items-center gap-1.5">
                        🎥 TikTok: la.casa.dulce62
                    </a>
                </div>
            </div>

        </section>

        <!-- ==================== PESTAÑA 2: ARMAR (EL SIMULADOR DINÁMICO) ==================== -->
        <section id="tabContent_armar" class="hidden grid grid-cols-1 lg:grid-cols-12 gap-6 items-start">
            
            <!-- VISTA PREVIA DEL VASO (IZQUIERDA) -->
            <div class="lg:col-span-5 bg-white rounded-3xl p-5 shadow-sm border border-dulceBox flex flex-col items-center justify-center min-h-[440px]">
                <h3 class="font-serif text-lg font-bold text-dulceBrownDark mb-1 text-center">Diseñador Virtual</h3>
                <p class="text-[10px] text-dulceAccent uppercase font-bold tracking-widest mb-6 text-center">¡Mira cómo se arma tu Pavé!</p>

                <!-- MODELADOR FÍSICO DEL VASO -->
                <div class="relative w-56 h-64 flex items-end justify-center mb-6">
                    
                    <!-- SALSAS EN LA PARED DEL VASO -->
                    <div id="v_salsa_Mora" class="absolute inset-x-3 top-20 h-24 bg-red-700/85 rounded-b-xl hidden z-20 mix-blend-multiply pointer-events-none">
                        <svg viewBox="0 0 100 50" class="w-full h-8 -mt-7 text-red-700/85 fill-current sauce-drip">
                            <path d="M 0,0 Q 15,18 30,5 Q 45,28 60,10 Q 75,32 90,5 L 100,0 L 100,50 L 0,50 Z"/>
                        </svg>
                    </div>
                    <div id="v_salsa_Chocolate" class="absolute inset-x-3 top-20 h-24 bg-[#3E2415]/95 rounded-b-xl hidden z-20 mix-blend-multiply pointer-events-none">
                        <svg viewBox="0 0 100 50" class="w-full h-8 -mt-7 text-[#3E2415]/95 fill-current sauce-drip">
                            <path d="M 0,0 Q 15,25 30,5 Q 45,30 60,10 Q 75,35 90,5 L 100,0 L 100,50 L 0,50 Z"/>
                        </svg>
                    </div>
                    <div id="v_salsa_Arequipe" class="absolute inset-x-3 top-20 h-24 bg-[#B58548]/95 rounded-b-xl hidden z-20 mix-blend-multiply pointer-events-none">
                        <svg viewBox="0 0 100 50" class="w-full h-8 -mt-7 text-[#B58548]/95 fill-current sauce-drip">
                            <path d="M 0,0 Q 10,20 25,5 Q 40,25 55,8 Q 70,30 85,5 L 100,0 L 100,50 L 0,50 Z"/>
                        </svg>
                    </div>
                    <div id="v_salsa_Lecherita" class="absolute inset-x-3 top-20 h-24 bg-[#FFFDF2]/90 rounded-b-xl hidden z-20 mix-blend-multiply pointer-events-none">
                        <svg viewBox="0 0 100 50" class="w-full h-8 -mt-7 text-[#FFFDF2]/90 fill-current sauce-drip">
                            <path d="M 0,0 Q 10,15 20,5 Q 40,25 60,8 Q 80,30 90,5 L 100,0 L 100,50 L 0,50 Z"/>
                        </svg>
                    </div>

                    <!-- CREMA BASE DE PAVÉ -->
                    <div id="v_crema_base" class="absolute inset-x-3 bottom-3 h-0 bg-gradient-to-t from-[#FFFDF8] to-[#F3ECD5] rounded-b-2xl border-t border-dulceBrownDark/10 hidden z-10 transition-all">
                        <!-- Chispas sutiles -->
                        <div class="absolute inset-0 opacity-10 bg-[radial-gradient(#4A3525_1.2px,transparent_1.2px)] [background-size:10px_10px]"></div>
                    </div>

                    <!-- CONTENEDOR DE INGREDIENTES QUE CAEN -->
                    <div id="v_toppings_container" class="absolute inset-x-3 top-10 bottom-3 z-30 pointer-events-none overflow-hidden rounded-b-2xl"></div>

                    <!-- DISEÑO DEL VASO DE CARTÓN KRAFT REAL -->
                    <div class="absolute bottom-0 w-full h-40 bg-[#D4B285] border-x-4 border-b-4 border-[#B9986D] rounded-b-3xl shadow-lg flex flex-col items-center justify-between py-4 overflow-hidden z-40">
                        <!-- Rayitas verticales sutiles del vaso de cartón -->
                        <div class="absolute inset-0 opacity-10 bg-[linear-gradient(90deg,transparent_50%,rgba(0,0,0,0.12)_50%)] bg-[size:6px_100%]"></div>
                        
                        <!-- Logo Sticker del Vaso -->
                        <div class="relative w-16 h-16 bg-white rounded-full border border-dulceGreen flex flex-col items-center justify-center p-1 shadow-inner z-10">
                            <span class="font-serif text-[7px] font-bold text-dulceGreen uppercase tracking-widest">LA CASA</span>
                            <span class="text-xs">🍓</span>
                            <span class="font-serif text-[7px] font-bold text-dulceGreen uppercase tracking-widest">DULCE</span>
                        </div>
                        <span class="text-[8px] font-extrabold uppercase tracking-widest text-dulceBrownLight/80 z-10">Presentación 8 Oz</span>
                    </div>
                </div>

                <!-- PRECIO DINÁMICO EN EL VASO -->
                <div class="text-center bg-[#FAF6F0] border border-dulceBox px-6 py-2 rounded-xl w-full">
                    <span class="text-[10px] font-bold text-dulceBrownLight/60 uppercase block">Total este postre</span>
                    <span id="visualTotal" class="font-serif text-2xl font-extrabold text-dulceGreen">$0</span>
                </div>
            </div>

            <!-- CONTROLES PASO A PASO (DERECHA) -->
            <div class="lg:col-span-7 bg-white rounded-3xl p-6 shadow-sm border border-dulceBox space-y-6">
                
                <!-- PASO A: BASE DEL PAVÉ -->
                <div>
                    <span class="text-[10px] font-extrabold uppercase tracking-widest text-dulceGold block mb-2">Paso 1: Elige la Base de tu postre</span>
                    <div class="grid grid-cols-2 gap-3">
                        <button onclick="seleccionarBaseWizard('clasico')" id="wizardBaseBtn_clasico" class="p-4 text-center rounded-2xl border-2 border-dulceBox hover:border-dulceGreen transition-all font-bold text-xs bg-white text-dulceBrownDark">
                            Bowl Clásico<br>
                            <span class="text-[10px] text-dulceGreen font-semibold">$13.000</span>
                        </button>
                        <button onclick="seleccionarBaseWizard('premium')" id="wizardBaseBtn_premium" class="p-4 text-center rounded-2xl border-2 border-dulceBox hover:border-dulceAccent transition-all font-bold text-xs bg-white text-dulceBrownDark">
                            Bowl Premium ⭐<br>
                            <span class="text-[10px] text-dulceAccent font-semibold">$14.000</span>
                        </button>
                    </div>
                </div>

                <!-- CONFIGURACIONES DINÁMICAS (Se muestran solo al tener base) -->
                <div id="wizardConfigSeccion" class="hidden space-y-6 border-t border-dulceBox pt-4">
                    
                    <!-- DINÁMICO: CONFIGURACIÓN SALSAS -->
                    <div>
                        <div class="flex justify-between items-center mb-2">
                            <span class="text-[10px] font-extrabold uppercase tracking-widest text-dulceGold block">Paso 2: Salsas de cortesía</span>
                            <span id="wizardSalsasContador" class="text-xs font-bold text-dulceAccent">0 de 0</span>
                        </div>
                        <div class="grid grid-cols-2 md:grid-cols-4 gap-2" id="wizardSalsasContainer"></div>
                    </div>

                    <!-- DINÁMICO: RECETA PREMIUM (Solo si base === premium) -->
                    <div id="wizardRecetaPremiumSeccion" class="hidden">
                        <span class="text-[10px] font-extrabold uppercase tracking-widest text-dulceGold block mb-2">Paso 3: Elige la Receta Premium</span>
                        <div class="grid grid-cols-3 gap-2">
                            <button onclick="seleccionarRecetaPremiumWizard('fresas')" id="wPremium_fresas" class="p-2.5 text-center rounded-xl border border-dulceBox hover:bg-dulceCream/40 text-[10px] font-bold text-dulceBrownDark">
                                🍓<br>Fresas + Queso
                            </button>
                            <button onclick="seleccionarRecetaPremiumWizard('oblea')" id="wPremium_oblea" class="p-2.5 text-center rounded-xl border border-dulceBox hover:bg-dulceCream/40 text-[10px] font-bold text-dulceBrownDark">
                                🥞<br>Oblea + Queso
                            </button>
                            <button onclick="seleccionarRecetaPremiumWizard('maracuya')" id="wPremium_maracuya" class="p-2.5 text-center rounded-xl border border-dulceBox hover:bg-dulceCream/40 text-[10px] font-bold text-dulceBrownDark">
                                💛<br>Maracuyá Cream
                            </button>
                        </div>
                    </div>

                    <!-- DINÁMICO: TOPPING INCLUIDO (Solo si base === clasico) -->
                    <div id="wizardToppingClasicoSeccion" class="hidden">
                        <span class="text-[10px] font-extrabold uppercase tracking-widest text-dulceGold block mb-2">Paso 3: Elige el topping clásico incluido</span>
                        <div class="grid grid-cols-2 md:grid-cols-3 gap-2" id="wizardToppingIncluidoContainer"></div>
                    </div>

                    <!-- ADICIONES EXTRA (OPCIONALES) -->
                    <div class="border-t border-dulceBox pt-4">
                        <span class="text-[10px] font-extrabold uppercase tracking-widest text-dulceGold block mb-2">Paso 4: Adiciones Extras (Opcional)</span>
                        
                        <div class="space-y-4">
                            <!-- Toppings Clásicos Extra -->
                            <div>
                                <h5 class="text-[10px] font-bold uppercase text-dulceGreen mb-2">Clásicos (+ $2.000 c/u)</h5>
                                <div class="grid grid-cols-2 md:grid-cols-3 gap-2" id="wizardToppingsExtraClasicos"></div>
                            </div>
                            <!-- Toppings Premium Extra -->
                            <div>
                                <h5 class="text-[10px] font-bold uppercase text-dulceAccent mb-2">Premium (+ $3.000 c/u)</h5>
                                <div class="grid grid-cols-2 md:grid-cols-2 gap-2" id="wizardToppingsExtraPremium"></div>
                            </div>
                        </div>
                    </div>

                    <!-- AGREGAR ESTA CREACIÓN AL CARRITO -->
                    <div class="border-t border-dulceBox pt-5 flex justify-end">
                        <button onclick="añadirPostreArmadoAlCarrito()" class="w-full bg-dulceGreen hover:bg-dulceGreenHover text-white py-3 rounded-2xl text-xs font-bold uppercase tracking-wider transition-all shadow-md flex items-center justify-center gap-1.5">
                            🛒 Agregar creación al carrito
                        </button>
                    </div>

                </div>

            </div>

        </section>

        <!-- ==================== PESTAÑA 3: CARRITO DE COMPRAS ==================== -->
        <section id="tabContent_carrito" class="hidden space-y-6">
            
            <div class="grid grid-cols-1 lg:grid-cols-12 gap-6 items-start">
                
                <!-- LISTA DE POSTRES EN EL CARRITO (IZQUIERDA) -->
                <div class="lg:col-span-7 bg-white rounded-3xl p-6 shadow-sm border border-dulceBox space-y-4">
                    <h3 class="font-serif text-xl font-bold text-dulceBrownDark border-b border-dulceBox pb-3 flex items-center justify-between">
                        <span>Tus Postres</span>
                        <span id="carritoItemsContador" class="text-xs bg-dulceGold/30 text-dulceBrownDark px-2.5 py-0.5 rounded-full font-bold">0 Items</span>
                    </h3>

                    <!-- Contenedor dinámico de items -->
                    <div id="carritoItemsList" class="space-y-3">
                        <!-- Vacío por defecto -->
                        <div class="text-center py-12 text-dulceBrownLight/60 text-xs font-bold">
                            📦 Tu carrito está vacío.<br>Explora la <span onclick="switchTab('carta')" class="text-dulceGold hover:underline cursor-pointer">Carta</span> o <span onclick="switchTab('armar')" class="text-dulceGold hover:underline cursor-pointer">Arma tu postre</span> para empezar.
                        </div>
                    </div>
                </div>

                <!-- FORMULARIO DE ENVÍO Y CONFIRMACIÓN (DERECHA) -->
                <div class="lg:col-span-5 bg-white rounded-3xl p-6 shadow-lg border border-dulceBox space-y-4">
                    <h3 class="font-serif text-xl font-bold text-dulceBrownDark border-b border-dulceBox pb-3 text-center">Confirmar Envío</h3>

                    <!-- ESPECIFICACIONES / EXCLUSIONES (Opción 1) -->
                    <div>
                        <label class="block text-xs font-bold mb-1 text-dulceBrownDark uppercase tracking-wider text-[10px]">Especificaciones Especiales (Sin queso, sin salsas...):</label>
                        <textarea id="cartEspecificaciones" rows="2" placeholder="Ej: Dos sin queso, enviar la oblea aparte..." class="w-full border border-dulceBox rounded-xl p-3 text-xs focus:outline-none focus:ring-2 focus:ring-dulceGold bg-dulceCream/25 text-dulceBrownLight font-medium transition-all"></textarea>
                    </div>

                    <!-- ENTREGA -->
                    <div class="grid grid-cols-2 gap-2">
                        <button onclick="cambiarEntregaCarrito('recoger')" id="cartBtnEntregaRecoger" class="p-2.5 text-center rounded-xl border-2 border-dulceGreen bg-dulceCream text-dulceGreen font-bold text-xs transition-all">
                            🏪 Recoger en Tienda
                        </button>
                        <button onclick="cambiarEntregaCarrito('domicilio')" id="cartBtnEntregaDomicilio" class="p-2.5 text-center rounded-xl border border-dulceBox hover:bg-dulceCream text-dulceBrownLight font-bold text-xs transition-all">
                            🛵 Domicilio (+$4.000)
                        </button>
                    </div>

                    <!-- DATOS -->
                    <div class="space-y-3">
                        <div>
                            <label class="block text-xs font-bold mb-1 text-dulceBrownLight">Nombre Completo *</label>
                            <input type="text" id="cartNombre" placeholder="¿A nombre de quién?" class="w-full border border-dulceBox rounded-xl p-2.5 text-xs focus:outline-none focus:ring-2 focus:ring-dulceGold text-dulceBrownLight bg-dulceCream/20 font-semibold">
                        </div>
                        <div id="cartDivDireccion" class="opacity-45 pointer-events-none">
                            <label class="block text-xs font-bold mb-1 text-dulceBrownLight">Dirección & Barrio *</label>
                            <input type="text" id="cartDireccion" placeholder="Ej: Calle 15 #23-45, Barrio San Vicente" class="w-full border border-dulceBox rounded-xl p-2.5 text-xs focus:outline-none focus:ring-2 focus:ring-dulceGold text-dulceBrownLight bg-dulceCream/20 font-semibold">
                        </div>
                    </div>

                    <!-- DESGLOSE DE VALORES -->
                    <div class="border-t border-dulceBox pt-3 space-y-1.5 text-xs text-dulceBrownLight">
                        <div class="flex justify-between">
                            <span>Subtotal Postres:</span>
                            <span id="cartSubtotal" class="font-bold">$0</span>
                        </div>
                        <div id="cartRowDomicilio" class="flex justify-between hidden">
                            <span class="flex items-center gap-1">🛵 Servicio de Domicilio:</span>
                            <span class="font-bold text-dulceAccent">+$4.000</span>
                        </div>
                    </div>

                    <!-- TOTAL COMPRA -->
                    <div class="border-t border-dulceBox pt-3 flex justify-between items-center text-dulceBrownDark">
                        <span class="font-extrabold text-sm">TOTAL DE COMPRA:</span>
                        <span id="cartTotalMonto" class="font-serif text-2xl font-extrabold text-dulceGreen">$0</span>
                    </div>

                    <!-- BOTÓN WHATSAPP -->
                    <button onclick="enviarPedidoCompletoPorWhatsApp()" class="w-full bg-dulceGreen hover:bg-dulceGreenHover text-white font-bold py-3.5 rounded-2xl flex items-center justify-center gap-2 shadow-md transition-all hover:scale-[1.01] active:scale-[0.99] text-xs uppercase tracking-wider">
                        <svg class="w-5 h-5 fill-current" viewBox="0 0 24 24">
                            <path d="M.057 24l1.687-6.163c-1.041-1.804-1.588-3.849-1.587-5.946C.06 5.348 5.397.01 12.008.01c3.202.001 6.212 1.246 8.477 3.514 2.266 2.268 3.507 5.28 3.505 8.484-.004 6.657-5.34 11.997-11.953 11.997-2.005-.001-3.973-.5-5.739-1.453L0 24zm6.59-4.846c1.6.95 3.188 1.449 4.825 1.451 5.436 0 9.86-4.37 9.864-9.799.002-2.63-1.023-5.101-2.885-6.966C16.59 1.988 14.113.96 11.487.959 6.052.959 1.628 5.33 1.624 10.762c-.001 1.702.447 3.364 1.3 4.8l-.995 3.636 3.718-.974zm11.774-7.902c-.31-.156-1.836-.906-2.12-.108-.284.1-.11.39-.143.14-.034.25-.135.53-.2.66-.065.13-.13.14-.44.015-.31-.156-1.31-.483-2.493-1.538-.918-.82-1.538-1.834-1.718-2.143-.18-.31-.019-.477.136-.632.14-.139.31-.36.464-.54.154-.18.206-.31.31-.515.103-.206.052-.386-.026-.54-.078-.155-.7-1.688-.957-2.31-.25-.6-.53-.52-.7-.52-.167-.003-.36-.003-.55-.003-.19 0-.5.07-.762.36-.263.29-1.006.98-1.006 2.4s1.033 2.78 1.176 2.97c.145.19 2.033 3.1 4.925 4.35.688.298 1.225.476 1.644.61.69.219 1.32.188 1.817.114.553-.082 1.836-.75 2.095-1.437.26-.687.26-1.277.18-1.4-.076-.124-.283-.2-.593-.356z"/>
                        </svg>
                        <span>Enviar Pedido por WhatsApp</span>
                    </button>
                    <p class="text-[9px] text-center text-dulceBrownLight/65">
                        Armamos tus postres al instante. El cobro final se acuerda directamente en WhatsApp.
                    </p>
                </div>

            </div>

        </section>

    </main>

    <!-- SISTEMA DE MODALES Y ALERTAS -->
    <div id="modalAlert" class="fixed inset-0 bg-black/55 z-50 flex items-center justify-center p-4 hidden">
        <div class="bg-white rounded-[32px] max-w-sm w-full p-6 text-center space-y-4 shadow-2xl border border-dulceBox">
            <div id="modalIcon" class="text-4xl">⚠️</div>
            <h4 id="modalTitle" class="font-serif text-lg font-bold text-dulceBrownDark">Atención</h4>
            <p id="modalMsg" class="text-xs text-dulceBrownLight font-semibold leading-relaxed">Completa el campo.</p>
            <button onclick="closeModal()" class="w-full bg-dulceBrownDark hover:bg-dulceBrownLight text-white font-bold py-3 rounded-2xl text-xs transition-all uppercase tracking-wider">
                Entendido
            </button>
        </div>
    </div>

    <!-- LOGICA DEL SISTEMA PREMIUM -->
    <script>
        const PRECIO_CLASICO = 13000;
        const PRECIO_PREMIUM = 14000;
        const COSTO_TOPPING_CLASICO = 2000;
        const COSTO_TOPPING_PREMIUM = 3000;
        const COSTO_DOMICILIO = 4000;

        const salsas = ["Arequipe", "Chocolate", "Lecherita", "Salsa de Mora 🍇"];
        const toppingsClasicos = ["Milo 🤎", "Leche Klim 🥛", "Brownie 🧁", "Galletas Ducales 🍪", "Quipito 🎉"];
        const toppingsPremium = ["Oblea 🥞", "Queso 🧀", "Fresas 🍓", "Crema de Maracuyá 💛"];

        const toppingEmojis = {
            "Milo 🤎": "🤎",
            "Leche Klim 🥛": "🥛",
            "Brownie 🧁": "🧁",
            "Galletas Ducales 🍪": "🍪",
            "Quipito 🎉": "🎉",
            "Oblea 🥞": "🥞",
            "Queso 🧀": "🧀",
            "Fresas 🍓": "🍓",
            "Crema de Maracuyá 💛": "💛"
        };

        // Estado de la Aplicación (Carrito & Simulador)
        let carrito = [];
        let tabActivo = 'carta';

        // Variables de estado del Simulador ("Armar")
        let simBase = null; // 'clasico' | 'premium'
        let simSalsas = []; // max 1 para clasico, max 2 para premium
        let simRecetaPremium = null; // 'fresas' | 'oblea' | 'maracuya'
        let simToppingIncluido = null; // topping clasico gratis si base clasico
        let simAdiciones = []; // toppings extra agregados

        // Datos del carrito
        let tipoEntregaCarrito = 'recoger';

        window.onload = function() {
            renderizarSalsasWizard();
            renderizarToppingIncluidoWizard();
            renderizarAdicionesWizard();
            actualizarTotalSimulacion();
            actualizarCarritoUI();
        }

        // --- SISTEMA DE PESTAÑAS (TABS) ---

        function switchTab(tabId) {
            tabActivo = tabId;
            const tabs = ['carta', 'armar', 'carrito'];
            
            tabs.forEach(t => {
                const btn = document.getElementById(`tabButton_${t}`);
                const content = document.getElementById(`tabContent_${t}`);
                if (t === tabId) {
                    btn.className = "flex-1 py-3 px-4 rounded-xl text-xs font-bold uppercase tracking-wider transition-all flex items-center justify-center gap-2 bg-dulceBrownDark text-white shadow-md";
                    content.classList.remove("hidden");
                } else {
                    btn.className = "flex-1 py-3 px-4 rounded-xl text-xs font-bold uppercase tracking-wider transition-all flex items-center justify-center gap-2 text-dulceBrownDark/70 hover:bg-white/40";
                    content.classList.add("hidden");
                }
            });

            if (tabId === 'carrito') {
                actualizarCarritoUI();
            }
        }

        // --- RENDERIZADORES DE WIZARD / SIMULADOR ---

        function renderizarSalsasWizard() {
            const container = document.getElementById("wizardSalsasContainer");
            container.innerHTML = salsas.map(s => `
                <button onclick="toggleSalsaWizard('${s}')" id="wSalsaBtn_${s.replace(/\s+/g, '')}" class="p-2.5 text-center text-[10px] font-bold rounded-xl border border-dulceBox hover:bg-dulceCream/40 transition-all bg-white text-dulceBrownDark">
                    ${s}
                </button>
            `).join('');
        }

        function renderizarToppingIncluidoWizard() {
            const container = document.getElementById("wizardToppingIncluidoContainer");
            container.innerHTML = toppingsClasicos.map(t => `
                <button onclick="seleccionarToppingIncluidoWizard('${t}')" id="wTopInclBtn_${t.replace(/\s+/g, '')}" class="p-2.5 text-center text-[10px] font-bold rounded-xl border border-dulceBox hover:bg-dulceCream/40 transition-all bg-white text-dulceBrownDark">
                    ${t}
                </button>
            `).join('');
        }

        function renderizarAdicionesWizard() {
            const containerClasicas = document.getElementById("wizardToppingsExtraClasicos");
            const containerPremium = document.getElementById("wizardToppingsExtraPremium");

            containerClasicas.innerHTML = toppingsClasicos.map(t => `
                <button onclick="toggleAdicionWizard('${t}', ${COSTO_TOPPING_CLASICO})" id="wAdicBtn_${t.replace(/\s+/g, '')}" class="p-2.5 text-center text-[10px] font-bold rounded-xl border border-dulceBox bg-white hover:bg-dulceCream/30 transition-all text-dulceBrownDark flex justify-between items-center px-3">
                    <span>${t}</span>
                    <span class="text-[9px] text-dulceAccent font-semibold">+$2.000</span>
                </button>
            `).join('');

            containerPremium.innerHTML = toppingsPremium.map(t => `
                <button onclick="toggleAdicionWizard('${t}', ${COSTO_TOPPING_PREMIUM})" id="wAdicBtn_${t.replace(/\s+/g, '')}" class="p-2.5 text-center text-[10px] font-bold rounded-xl border border-dulceBox bg-white hover:bg-dulceCream/30 transition-all text-dulceBrownDark flex justify-between items-center px-3">
                    <span>${t}</span>
                    <span class="text-[9px] text-dulceAccent font-semibold">+$3.000</span>
                </button>
            `).join('');
        }

        // --- SISTEMA DEL SIMULADOR ("ARMAR") ---

        function seleccionarBaseWizard(base) {
            simBase = base;
            
            // UI de los botones de base
            const btnClasico = document.getElementById("wizardBaseBtn_clasico");
            const btnPremium = document.getElementById("wizardBaseBtn_premium");

            if (base === 'clasico') {
                btnClasico.className = "p-4 text-center rounded-2xl border-2 border-dulceGreen bg-dulceCream/30 transition-all font-bold text-xs text-dulceGreen shadow-md";
                btnPremium.className = "p-4 text-center rounded-2xl border-2 border-dulceBox hover:border-dulceAccent transition-all font-bold text-xs bg-white text-dulceBrownDark";
                
                document.getElementById("wizardToppingClasicoSeccion").classList.remove("hidden");
                document.getElementById("wizardRecetaPremiumSeccion").classList.add("hidden");
                
                resetSimuladorPremiumOptions();
            } else {
                btnPremium.className = "p-4 text-center rounded-2xl border-2 border-dulceAccent bg-dulceCream/30 transition-all font-bold text-xs text-dulceAccent shadow-md";
                btnClasico.className = "p-4 text-center rounded-2xl border-2 border-dulceBox hover:border-dulceGreen transition-all font-bold text-xs bg-white text-dulceBrownDark";
                
                document.getElementById("wizardToppingClasicoSeccion").classList.add("hidden");
                document.getElementById("wizardRecetaPremiumSeccion").classList.remove("hidden");
                
                resetSimuladorClasicoOptions();
            }

            document.getElementById("wizardConfigSeccion").classList.remove("hidden");
            document.getElementById("wizardSalsasContador").innerText = `0 de ${base === 'clasico' ? 1 : 2}`;

            // Mostrar crema del vaso
            const cream = document.getElementById("v_crema_base");
            cream.classList.remove("hidden");
            cream.style.height = "75%";

            actualizarTotalSimulacion();
        }

        function toggleSalsaWizard(salsa) {
            if (!simBase) return;
            const maxSalsas = simBase === 'clasico' ? 1 : 2;
            const btn = document.getElementById(`wSalsaBtn_${salsa.replace(/\s+/g, '')}`);

            if (simSalsas.includes(salsa)) {
                simSalsas = simSalsas.filter(s => s !== salsa);
                btn.className = "p-2.5 text-center text-[10px] font-bold rounded-xl border border-dulceBox hover:bg-dulceCream/40 transition-all bg-white text-dulceBrownDark";
            } else {
                if (simSalsas.length < maxSalsas) {
                    simSalsas.push(salsa);
                    const colorBtnClass = simBase === 'clasico' ? 'bg-dulceGreen text-white border-dulceGreen' : 'bg-dulceAccent text-white border-dulceAccent';
                    btn.className = `p-2.5 text-center text-[10px] font-bold rounded-xl border-2 transition-all shadow-sm ${colorBtnClass}`;
                } else {
                    abrirModal("Límite superado", `La base elegida incluye un máximo de ${maxSalsas} salsa(s) de cortesía.`, "🍯");
                }
            }

            document.getElementById("wizardSalsasContador").innerText = `${simSalsas.length} de ${maxSalsas}`;
            actualizarTotalSimulacion();
        }

        function seleccionarToppingIncludedWizard(topping) {
            simToppingIncluido = topping;
            toppingsClasicos.forEach(t => {
                const btn = document.getElementById(`wTopInclBtn_${t.replace(/\s+/g, '')}`);
                if (t === topping) {
                    btn.className = "p-2.5 text-center text-[10px] font-bold rounded-xl border-2 border-dulceGreen bg-dulceCream/50 transition-all text-dulceGreen shadow-sm";
                    agregarToppingAlVasoVisual(t);
                } else {
                    btn.className = "p-2.5 text-center text-[10px] font-bold rounded-xl border border-dulceBox hover:bg-dulceCream/40 transition-all bg-white text-dulceBrownDark";
                    removerToppingDelVasoVisual(t);
                }
            });
            actualizarTotalSimulacion();
        }

        // Alias for backwards compatibility
        const seleccionarToppingIncluidoWizard = seleccionarToppingIncludedWizard;

        function seleccionarRecetaPremiumWizard(receta) {
            simRecetaPremium = receta;
            const recetas = ['fresas', 'oblea', 'maracuya'];
            recetas.forEach(r => {
                const btn = document.getElementById(`wPremium_${r}`);
                if (r === receta) {
                    btn.className = "p-2.5 text-center rounded-xl border-2 border-dulceAccent bg-dulceCream/50 transition-all text-[10px] font-bold text-dulceAccent shadow-sm";
                } else {
                    btn.className = "p-2.5 text-center rounded-xl border border-dulceBox hover:bg-dulceCream/40 text-[10px] font-bold text-dulceBrownDark";
                }
            });

            // Limpiar Toppings visuales de recetas viejas
            removerToppingDelVasoVisual("Fresas 🍓");
            removerToppingDelVasoVisual("Queso 🧀");
            removerToppingDelVasoVisual("Oblea 🥞");
            removerToppingDelVasoVisual("Crema de Maracuyá 💛");

            if (receta === 'fresas') {
                agregarToppingAlVasoVisual("Fresas 🍓");
                agregarToppingAlVasoVisual("Queso 🧀");
            } else if (receta === 'oblea') {
                agregarToppingAlVasoVisual("Oblea 🥞");
                agregarToppingAlVasoVisual("Queso 🧀");
            } else if (receta === 'maracuya') {
                agregarToppingAlVasoVisual("Crema de Maracuyá 💛");
            }

            actualizarTotalSimulacion();
        }

        function toggleAdicionWizard(topping, precio) {
            const btn = document.getElementById(`wAdicBtn_${topping.replace(/\s+/g, '')}`);
            const index = simAdiciones.findIndex(a => a.nombre === topping);

            if (index > -1) {
                simAdiciones.splice(index, 1);
                btn.className = "p-2.5 text-center text-[10px] font-bold rounded-xl border border-dulceBox bg-white hover:bg-dulceCream/30 transition-all text-dulceBrownDark flex justify-between items-center px-3";
                removerToppingDelVasoVisual(topping);
            } else {
                simAdiciones.push({ nombre: topping, precio });
                btn.className = "p-2.5 text-center text-[10px] font-bold rounded-xl border-2 border-dulceGold bg-dulceCream/50 transition-all text-dulceBrownDark flex justify-between items-center px-3 shadow-sm";
                agregarToppingAlVasoVisual(topping);
            }

            actualizarTotalSimulacion();
        }

        // --- SISTEMA DE VISUALIZACIÓN DINÁMICA DEL VASO ---

        function actualizarTotalSimulacion() {
            let total = 0;
            if (simBase) {
                total = simBase === 'clasico' ? PRECIO_CLASICO : PRECIO_PREMIUM;
            }
            simAdiciones.forEach(a => total += a.precio);

            // Reflejar en la UI
            document.getElementById("visualTotal").innerText = `$${total.toLocaleString()}`;

            // Control de Salsas Visuales cayendo en el Vaso
            document.getElementById("v_salsa_Mora").classList.add("hidden");
            document.getElementById("v_salsa_Chocolate").classList.add("hidden");
            document.getElementById("v_salsa_Arequipe").classList.add("hidden");
            document.getElementById("v_salsa_Lecherita").classList.add("hidden");

            simSalsas.forEach(s => {
                if (s.includes("Mora")) document.getElementById("v_salsa_Mora").classList.remove("hidden");
                if (s.includes("Chocolate")) document.getElementById("v_salsa_Chocolate").classList.remove("hidden");
                if (s.includes("Arequipe")) document.getElementById("v_salsa_Arequipe").classList.remove("hidden");
                if (s.includes("Lecherita")) document.getElementById("v_salsa_Lecherita").classList.remove("hidden");
            });
        }

        function agregarToppingAlVasoVisual(topping) {
            const container = document.getElementById("v_toppings_container");
            const elementId = `v_item_${topping.replace(/\s+/g, '')}`;

            // No duplicar
            if (document.getElementById(elementId)) return;

            const emoji = toppingEmojis[topping] || "🍫";

            // Posiciones azarosas en el vaso para simular dispersión real de ingredientes
            const x = Math.floor(Math.random() * 65) + 12; // 12% a 77%
            const y = Math.floor(Math.random() * 40) + 35; // 35% a 75%
            const deg = Math.floor(Math.random() * 60) - 30; // -30deg a 30deg

            const div = document.createElement("div");
            div.id = elementId;
            div.className = "absolute text-3xl z-30 topping-drop pointer-events-none select-none";
            div.style.left = `${x}%`;
            div.style.top = `${y}%`;
            div.style.transform = `rotate(${deg}deg)`;
            div.innerText = emoji;

            container.appendChild(div);
        }

        function removerToppingDelVasoVisual(topping) {
            const el = document.getElementById(`v_item_${topping.replace(/\s+/g, '')}`);
            if (el) el.remove();
        }

        function resetSimuladorClasicoOptions() {
            simSalsas = [];
            simToppingIncluido = null;
            renderizarSalsasWizard();
            renderizarToppingIncluidoWizard();
        }

        function resetSimuladorPremiumOptions() {
            simSalsas = [];
            simRecetaPremium = null;
            renderizarSalsasWizard();
            const recetas = ['fresas', 'oblea', 'maracuya'];
            recetas.forEach(r => document.getElementById(`wPremium_${r}`).className = "p-2.5 text-center rounded-xl border border-dulceBox hover:bg-dulceCream/40 text-[10px] font-bold text-dulceBrownDark");
            limpiarToppingsRecetaVisual();
        }

        function limpiarToppingsRecetaVisual() {
            removerToppingDelVasoVisual("Fresas 🍓");
            removerToppingDelVasoVisual("Queso 🧀");
            removerToppingDelVasoVisual("Oblea 🥞");
            removerToppingDelVasoVisual("Crema de Maracuyá 💛");
        }

        // --- ENLACE CARTA -> SIMULADOR ---

        function personalizarPostreEnSimulador(baseType, recetaKey) {
            switchTab('armar');
            seleccionarBaseWizard(baseType);
            if (baseType === 'premium' && recetaKey) {
                seleccionarRecetaPremiumWizard(recetaKey);
            }
        }

        // --- SISTEMA DEL CARRITO DE COMPRAS ---

        function comprarPostrePredefinido(nombre, precio, ...toppingsIncluidos) {
            // Añadir directo al carrito
            const postre = {
                nombre: nombre,
                precio: precio,
                detalles: `Incluye: ${toppingsIncluidos.join(', ')}.`,
                salsas: ["Elegidas al entregar"],
                adiciones: [],
                tipo: 'predefinido'
            };
            
            carrito.push(postre);
            actualizarCarritoUI();
            
            abrirModal("🛒 Añadido", `¡Hemos agregado "${nombre}" a tu Carrito con éxito!`, "🧁");
        }

        function añadirPostreArmadoAlCarrito() {
            // Validaciones de simulador
            if (!simBase) {
                abrirModal("Base Requerida", "Selecciona una base para tu Pavé (Clásico o Premium).", "🧁");
                return;
            }

            if (simBase === 'clasico') {
                if (simSalsas.length === 0) {
                    abrirModal("Falta la Salsa", "Selecciona la salsa incluida de cortesía en tu Pavé Clásico.", "🍯");
                    return;
                }
                if (!simToppingIncluido) {
                    abrirModal("Falta el Topping", "Selecciona el topping incluido de cortesía en tu Pavé Clásico.", "🍫");
                    return;
                }
            } else {
                if (!simRecetaPremium) {
                    abrirModal("Receta Requerida", "Elige una combinación Premium (Fresas, Oblea o Maracuyá).", "🍓");
                    return;
                }
            }

            // Construir el postre personalizado
            let nombrePostre = simBase === 'clasico' ? "Pavé Clásico Diseñado" : "Pavé Premium Diseñado";
            let precioBase = simBase === 'clasico' ? PRECIO_CLASICO : PRECIO_PREMIUM;
            
            let detallesText = "";
            if (simBase === 'clasico') {
                detallesText = `Salsa: ${simSalsas.join(', ')} | Topping: ${simToppingIncluido}`;
            } else {
                let recName = simRecetaPremium === 'fresas' ? 'Fresas con Cream' : simRecetaPremium === 'oblea' ? 'Oblea Especial' : 'Crema de Maracuyá';
                detallesText = `Receta: ${recName} | Salsas: ${simSalsas.length > 0 ? simSalsas.join(', ') : 'Ninguna'}`;
            }

            const postrePersonalizado = {
                nombre: nombrePostre,
                precio: precioBase + simAdiciones.reduce((acc, a) => acc + a.precio, 0),
                detalles: detallesText,
                salsas: [...simSalsas],
                adiciones: [...simAdiciones],
                tipo: 'personalizado'
            };

            carrito.push(postrePersonalizado);
            
            // Resetear Simulador
            simBase = null;
            simSalsas = [];
            simRecetaPremium = null;
            simToppingIncluido = null;
            simAdiciones = [];
            
            // Limpiar vaso
            document.getElementById("v_crema_base").classList.add("hidden");
            document.getElementById("v_toppings_container").innerHTML = "";
            document.getElementById("wizardConfigSeccion").classList.add("hidden");
            document.getElementById("wizardBaseBtn_clasico").className = "p-4 text-center rounded-2xl border-2 border-dulceBox hover:border-dulceGreen transition-all font-bold text-xs bg-white text-dulceBrownDark";
            document.getElementById("wizardBaseBtn_premium").className = "p-4 text-center rounded-2xl border-2 border-dulceBox hover:border-dulceAccent transition-all font-bold text-xs bg-white text-dulceBrownDark";
            renderizarAdicionesWizard();

            actualizarTotalSimulacion();
            actualizarCarritoUI();

            abrirModal("🛒 Postre Diseñado", "¡Postre personalizado añadido al carrito de compras!", "🎉");
            switchTab('carrito');
        }

        function actualizarCarritoUI() {
            const listContainer = document.getElementById("carritoItemsList");
            const badgeCount = document.getElementById("badgeCarritoCount");
            const itemsCount = document.getElementById("carritoItemsContador");
            const cartTotalMonto = document.getElementById("cartTotalMonto");
            const cartSubtotal = document.getElementById("cartSubtotal");
            const cartRowDomicilio = document.getElementById("cartRowDomicilio");

            // Cantidad de elementos
            badgeCount.innerText = carrito.length;
            if (carrito.length > 0) {
                badgeCount.classList.remove("hidden");
                itemsCount.innerText = `${carrito.length} Items`;
            } else {
                badgeCount.classList.add("hidden");
                itemsCount.innerText = "0 Items";
            }

            if (carrito.length === 0) {
                listContainer.innerHTML = `
                    <div class="text-center py-12 text-dulceBrownLight/60 text-xs font-bold">
                        📦 Tu carrito está vacío.<br>Explora la <span onclick="switchTab('carta')" class="text-dulceGold hover:underline cursor-pointer">Carta</span> o <span onclick="switchTab('armar')" class="text-dulceGold hover:underline cursor-pointer">Arma tu postre</span> para empezar.
                    </div>
                `;
                cartSubtotal.innerText = "$0";
                cartRowDomicilio.classList.add("hidden");
                cartTotalMonto.innerText = "$0";
                return;
            }

            let subtotal = 0;

            listContainer.innerHTML = carrito.map((item, index) => {
                subtotal += item.precio;

                let extrasText = "";
                if (item.adiciones && item.adiciones.length > 0) {
                    extrasText = `<div class="text-[10px] text-dulceAccent font-semibold mt-1">➕ Extras: ${item.adiciones.map(a => `${a.nombre}`).join(', ')}</div>`;
                }

                return `
                    <div class="p-4 bg-dulceCream/45 border border-dulceBox rounded-2xl flex justify-between items-center gap-4">
                        <div class="space-y-0.5">
                            <h4 class="font-bold text-sm text-dulceBrownDark">${item.nombre}</h4>
                            <p class="text-[11px] text-dulceBrownLight leading-tight font-medium">${item.detalles}</p>
                            ${extrasText}
                        </div>
                        <div class="text-right flex flex-col items-end gap-2">
                            <span class="font-serif text-sm font-bold text-dulceGreen">$${item.precio.toLocaleString()}</span>
                            <button onclick="eliminarDelCarrito(${index})" class="text-[10px] text-red-500 font-bold hover:underline">
                                Eliminar
                            </button>
                        </div>
                    </div>
                `;
            }).join('');

            cartSubtotal.innerText = `$${subtotal.toLocaleString()}`;

            // Calcular total con Domicilio si aplica
            let totalGeneral = subtotal;
            if (tipoEntregaCarrito === 'domicilio') {
                totalGeneral += COSTO_DOMICILIO;
                cartRowDomicilio.classList.remove("hidden");
            } else {
                cartRowDomicilio.classList.add("hidden");
            }

            cartTotalMonto.innerText = `$${totalGeneral.toLocaleString()}`;
        }

        function eliminarDelCarrito(index) {
            carrito.splice(index, 1);
            actualizarCarritoUI();
        }

        function cambiarEntregaCarrito(tipo) {
            tipoEntregaCarrito = tipo;
            const btnRecoger = document.getElementById("cartBtnEntregaRecoger");
            const btnDomicilio = document.getElementById("cartBtnEntregaDomicilio");
            const divDir = document.getElementById("cartDivDireccion");

            if (tipo === 'recoger') {
                btnRecoger.className = "p-2.5 text-center rounded-xl border-2 border-dulceGreen bg-dulceCream text-dulceGreen font-bold text-xs transition-all";
                btnDomicilio.className = "p-2.5 text-center rounded-xl border border-dulceBox hover:bg-dulceCream text-dulceBrownLight font-bold text-xs transition-all";
                divDir.classList.add("opacity-45", "pointer-events-none");
                document.getElementById("cartDireccion").value = "";
            } else {
                btnDomicilio.className = "p-2.5 text-center rounded-xl border-2 border-dulceGreen bg-dulceCream text-dulceGreen font-bold text-xs transition-all";
                btnRecoger.className = "p-2.5 text-center rounded-xl border border-dulceBox hover:bg-dulceCream text-dulceBrownLight font-bold text-xs transition-all";
                divDir.classList.remove("opacity-45", "pointer-events-none");
            }
            actualizarCarritoUI();
        }

        // --- ENVÍO DEL PEDIDO COMPLETO A WHATSAPP ---

        function enviarPedidoCompletoPorWhatsApp() {
            if (carrito.length === 0) {
                abrirModal("Carrito Vacío", "Por favor agrega al menos un postre para poder procesar tu pedido.", "🛒");
                return;
            }

            const nombre = document.getElementById("cartNombre").value.trim();
            const direccion = document.getElementById("cartDireccion").value.trim();
            const especificaciones = document.getElementById("cartEspecificaciones").value.trim();

            if (!nombre) {
                abrirModal("Falta tu nombre", "Escribe tu nombre para saber quién reclama o recibe el pedido.", "👤");
                return;
            }

            if (tipoEntregaCarrito === 'domicilio' && !direccion) {
                abrirModal("Dirección Requerida", "Escribe tu dirección de entrega para programar el domicilio.", "📍");
                return;
            }

            // Construir el mensaje formateado para WhatsApp
            let mensaje = `🏠 *¡NUEVO PEDIDO - LA CASA DULCE!*\n\n`;
            mensaje += `👤 *Cliente:* ${nombre}\n`;
            mensaje += `🛵 *Entrega:* ${tipoEntregaCarrito === 'recoger' ? '🏪 Recoger en Tienda Física' : '🛵 Solicitar Domicilio'}\n`;
            if (tipoEntregaCarrito === 'domicilio') {
                mensaje += `📍 *Dirección:* ${direccion}\n`;
            }
            
            mensaje += `\n─────────────────────\n`;
            mensaje += `🍨 *DETALLE DE TU PEDIDO:*\n\n`;

            let subtotalAcumulado = 0;

            carrito.forEach((postre, i) => {
                subtotalAcumulado += postre.precio;
                mensaje += `*${i+1}. ${postre.nombre}* ($${postre.precio.toLocaleString()})\n`;
                mensaje += `  _${postre.detalles}_\n`;
                
                if (postre.adiciones && postre.adiciones.length > 0) {
                    mensaje += `  _Extras: ${postre.adiciones.map(a => a.nombre).join(', ')}_\n`;
                }
                mensaje += `\n`;
            });

            mensaje += `─────────────────────\n`;
            mensaje += `💵 *Subtotal Postres:* $${subtotalAcumulado.toLocaleString()}\n`;
            
            let totalGeneral = subtotalAcumulado;
            if (tipoEntregaCarrito === 'domicilio') {
                totalGeneral += COSTO_DOMICILIO;
                mensaje += `🛵 *Servicio de Domicilio:* $${COSTO_DOMICILIO.toLocaleString()}\n`;
            }
            
            if (especificaciones) {
                mensaje += `\n⚠️ *ESPECIFICACIONES:* "${especificaciones}"\n`;
            }

            mensaje += `─────────────────────\n`;
            mensaje += `💰 *TOTAL GENERAL:* $${totalGeneral.toLocaleString()}\n\n`;
            mensaje += ` _He armado mi pedido usando el catálogo virtual de La Casa Dulce. ¿Confirmamos para preparar?_ ✨`;

            const url = `https://wa.me/573128903511?text=${encodeURIComponent(mensaje)}`;
            window.open(url, '_blank');
        }

        // --- CONTROLADORES DE MODALES ---

        function abrirModal(titulo, mensaje, icono = "⚠️") {
            document.getElementById("modalIcon").innerText = icono;
            document.getElementById("modalTitle").innerText = titulo;
            document.getElementById("modalMsg").innerText = mensaje;
            document.getElementById("modalAlert").classList.remove("hidden");
        }

        function closeModal() {
            document.getElementById("modalAlert").classList.add("hidden");
        }
    </script>
</body>
</html>

```
